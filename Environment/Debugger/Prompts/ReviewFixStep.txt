FIX PROPOSAL REVIEW
You are proposing a fix for [[function]]. Below is the recent Git history for this function.

GIT HISTORY FOR FUNCTION [[function]] START
[[git_history]]
GIT HISTORY FOR FUNCTION [[function]] END

Context: This codebase is managed autonomously by an AI coding agent. Unlike human-managed repos where each commit typically represents an intentional feature or fix, this history reflects an evolutionary debugging process. 
- First phase of the process is code generation
- Then syntactical correctness
- Finally debugging. During debugging we have multiple sessions, usually for unit and integration tests. Each debug session has it's own steps sequence, so you can see out of order debug steps in the commit history (from different sessions)

Commits fall into four categories:
1. Code generation — initial code generation with function description (the description might be outdated due to subsequent code modifications)
2. Breakthrough fixes — unblocked significant debugging progress
3. Contributing fixes — moved the trajectory forward incrementally
4. Dead ends — did not help or even caused regressions (later reverted or superseded)

When analyzing this history:
- Look for patterns in breakthrough fixes — what made them successful?
- Identify dead ends and understand why they failed, not just that they failed
- Note any oscillation patterns (fix → revert → different fix) which may indicate a deeper architectural issue
- Don't assume recency equals correctness — a recent commit may itself be a dead end

Based on your analysis, provide a refined fix suggestion. Concisely explain how your approach avoids past dead ends (if any) and builds on what previously worked.

The information in the new 'motivation' section has to be self contained. It is crucial to summarize all important details as this will be the only information available in the next steps. Preserve all specific details that actually led to this decision. Ensure the exact fix is explained with enough implementation details. You can adjust the motivation base on the Git history but avoid quoting it directly. Use it more like you already have that intuition.

Review the 'PROJECT SOURCE CODE REQUIREMENTS' section since those are hard requirements and will be enforced in the function implementation. Pay attention to the requirements for data flow and variable declarations. If the requirements contradict the suggested fix, consider adjusting the fix strategy or, if that is not possible, selecting a different type of action for the next step.