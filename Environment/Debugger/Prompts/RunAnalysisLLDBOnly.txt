[[app_info]]

APPLICATION RUN ANALYSIS

I've executed the '[[application]]' application under LLDB performing the test as described in the 'TEST DESCRIPTION' section

So far, we’ve only analyzed sections of the log that included application stdout/stderr output. Because the full log is quite large, our previous analysis was based on snippets from the beginning, middle, and end of that combined log. Here is our current progress based on those snippets:

//Current progress start
[[progress]]
//Current progress end

Now, please review the full LLDB-only log from the same run!

I've executed the '[[application]]' application under LLDB in batch mode to collect debug information
For that purpose I set a breakpoint at entering and existing each application function. Each breakpoint optionally might have commands for providing information about process status, thread info, thread backtrace, frame select and frame variable. Due to limitation in the size of the lldb trace log, all breakpoints have an implicit condition to hit (and log) only the first 3 times. 

I will ask you to analyze the lldb debug log. The log contains debug information provided by LLDB

//LLDB log starts here
[[lldb_log]]
//LLDB log ends here

Using the new LLDB log, compare and “map” what we learned from the application log analysis to this full LLDB trace. Since this debugging information is from the entire program run, try to confirm if there are any issues highlighted in the previous steps. Check for oversights in the progress so far. Verify that the code execution follows the correct path, as expected for the test scenario. You can consult the application call graph and function listings. Try to understand whether each function was called with the right input arguments and if it returned the correct results. In case of timed out execution break, try to analyze where the application hangs.

Update your debug notes. We are going to use this information later to decide what would be the next step debugging the application. If the unit test is successful the 'debug_notes' field must have only one word - "PASS"

Update the log summary. In the log summary add quotes from the new log section that are relevant to the finding collected in the new debug notes.

While analyzing the LLDB trace log, if you identify any issues, update the lists with application-defined custom functions, custom data types, and files that are relevant to those issues and are worth further investigation. For each field ('functions', 'data_types', 'test_files'), you can select up to 5 items, including any previously selected items. Information for the listed functions, data types and files will be provided later. If no issues you can leave the corresponding list empty.

Ideally, 'debug_notes' and 'log_summary' fields have to be 3 paragraphs or fewer, less than 2048 characters each, but this is not a hard limit as we don't want to miss something important.