I've tried to compile the function '[[function]]'

[[function_ctx]]

'[[function]]' has been compiled with the following command line:

[[command]]

Here is the output from the Clang compiler:

[[output]]

Review the output for errors and try to fix the compilation by modifying ONLY the '[[function]]' source. Don't modify data type definitions and other functions.

Here is the list of 'available data types and functions' for the implementation of '[[function]]'

[[api]]

Note - for the implementation of '[[function]]' you can use 'available data types and functions' exactly as they are listed above. You can't assume or introduce changes in their declarations or function signatures.
[[panic]]
And here is the review checklist:

- Don't modify the names of existing functions and data types.
- Provide full implementation of the reviewed function, no placeholders and stubs, no "TO DO" comments, no empty scopes.
- Don't provide include statements in the source. I will add them later.
- Don't introduce new functions and data type definitions.
- Don't predefine the PRINT_TEST macro
- Don't declare extern and global variables and data structures! To pass the data, use function parameters and result instead.
- Don't declare, define and implement any other functions except the listed above.
- Try to introduce non-destructive changes, so that the functions that are called by '[[function]]' will continue to link and work properly.
- Don't modify structures, classes and enums defined in the 'available libraries'.
- Avoid lambda expressions in the implementation. If not possible to implement everything by using available functions and libraries, consider calling a new helper function. Just call the new function, we'll declare and define it later based on the usage.
- All produced or updated data from this function that will be accessible outside of the scope of the function must be exposed by the return result or mutable arguments.
- Using global variables from 'available libraries' is permitted!
- When using the C++ Standard Library don't assume the 'std' and other namespaces from STD library are defined. You must prefix all appropriate types and functions form the STD library with their namespace
- Don't assume this software declares and defines its own C++ classes. This software defines only 'struct' and 'enum' types of data. Classes might be used only from the available libraries
- Classes and inheritance aren't allowed. When required to design custom structures that combine characteristics from other data types, use composition.
- In the data structures, defined by this software, don't define member functions, constructors and destructors.
- In the data structures, defined by this software, don't define members that are function pointers of std::function<> types.
- Don't define nested data structures and types.
- Don't use exception handling!
- Don't use preprocessor directives in the provided source.
- Don't use the 'auto' keyword for data types that are defined in this software. It is fine to use 'auto' for data types from 'available libraries'.
- Consult with available libraries section how to leverage functionality provided by the libraries when appropriate.
- You must not use functions from external libraries, except from libraries listed in 'AVAILABLE LIBRARIES' section
- The code you generate will be compiled with the Clang compiler
- Use PRINT_TEST(fmt, args...) to print as much verbose information as required for the purpose of unit tests. Assume the definition of the macro is available. The definition will be empty in release builds. Unit test code will be implemented on a separate step. Now implement only the function.
- Use std::cout with the << operator (avoid other methods) for output that should persist in release builds (user-facing messages, final results). Use PRINT_TEST for debug/diagnostic output that will be stripped from release builds.
- Provide the code in your response in markdown syntax. Prefix the C++ source code with ```cpp and postfix the code in your response with ```
- Based on this checklist, provide the best answer without asking questions

In your response, provide ONLY the modified definitions of '[[function]]'
Now go ahead fix '[[function]]'